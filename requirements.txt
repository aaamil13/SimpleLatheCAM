# Core CAM dependencies
pyclipper>=1.3.0          # 2D polygon offset and boolean ops (roughing passes)

# GUI
PySide6>=6.6.0            # Qt6 Python bindings (standalone editor)

# 3D preview (on-demand)
cadquery>=2.4.0           # OpenCASCADE parametric geometry for 3D revolve preview

# Development / testing
pytest>=8.0.0
